# OPUS 67 v6.0.0 - MASTER REGISTRY
# Single Source of Truth - Generated 2024-12-06
# This file orchestrates ALL OPUS 67 components

version: "6.0.0"
codename: "The Unification"
generated: "2024-12-06T00:00:00Z"

# ============================================================================
# COMPONENT COUNTS (Updated at boot)
# ============================================================================
meta:
  skills_count: 141
  agents_count: 107
  modes_count: 30
  mcps_count: 82
  hooks_count: 5

# ============================================================================
# SKILLS CONFIGURATION
# ============================================================================
skills:
  source: "../skills/registry.yaml"
  metadata_dir: "../skills/metadata/"
  definitions_dir: "../skills/definitions/"
  loaded_at_boot: true
  auto_detect: true
  max_concurrent: 5
  token_budget: 50000

# ============================================================================
# AGENTS CONFIGURATION
# ============================================================================
agents:
  source: "../../../.claude/agents/"
  format: "markdown"
  loaded_at_boot: true
  system_agents:
    - "../agents/learning-observer.yaml"
    - "../agents/skills-navigator.yaml"

# ============================================================================
# MODES CONFIGURATION
# ============================================================================
modes:
  source: "../modes/registry.yaml"
  default: "AUTO"
  loaded_at_boot: true
  available:
    - ULTRA    # Maximum intelligence
    - THINK    # Deep analysis
    - BUILD    # Production code
    - VIBE     # Rapid prototyping
    - LIGHT    # Minimal overhead
    - CREATIVE # Design & content
    - DATA     # Analytics & research
    - AUDIT    # Security & quality
    - SWARM    # Multi-agent
    - AUTO     # Intelligent switching

# ============================================================================
# MCP CONFIGURATION (THE KEY FIX)
# ============================================================================
mcps:
  source: "../mcp/connections.yaml"
  auto_connect: true
  register_in_claude: true  # This triggers settings.json update
  connection_timeout_ms: 5000
  retry_attempts: 3
  categories:
    - blockchain
    - social
    - data
    - productivity
    - ai_search
    - memory
    - solana_stack
    - ai_ml
    - evm_chains
    - databases
    - monitoring
    - design
    - project_management
    - communication
    - hosting
    - payments
    - web3
    - realtime

# ============================================================================
# HOOKS CONFIGURATION
# ============================================================================
hooks:
  source: "../../../.claude/hooks/"
  active:
    - session-start.js
    - opus67-auto-detect.js
    - pre-bash.js
    - post-write.js
    - post-bash.js
  visibility: true  # Log hook activity

# ============================================================================
# BOOT SEQUENCE
# ============================================================================
boot:
  order:
    - registry      # Load this file
    - door          # Inject THE DOOR
    - mcps          # Register MCPs in Claude settings
    - skills        # Load skills registry
    - agents        # Load agents
    - modes         # Load modes
    - hooks         # Activate hooks
    - context       # Index workspace
  display_banner: true
  verify_access: true

# ============================================================================
# THE DOOR CONFIGURATION
# ============================================================================
door:
  source: "../THE_DOOR.md"
  version: "5.1.0"
  inject_to_claude: true
  marker: "<!-- OPUS 67 THE DOOR -->"

# ============================================================================
# UNIFIED SYSTEM FLAGS
# ============================================================================
unified:
  version: "6.0.0"
  single_boot_command: true
  consistent_loading: true
  all_mcps_registered: true
  no_duplicate_doors: true
  hooks_visible: true

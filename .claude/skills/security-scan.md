# Security Scan

Scan project for security issues without fixing them.

Detects:
• Exposed API keys in .env
• Missing firewall/CSP config
• Unencrypted credentials
• Vulnerable dependencies

Usage: `/security-scan` or `pnpm security:scan`
